version: "3.9"
services:
  backend:
    build: ./backend
    container_name: agent_hub_backend
    restart: unless-stopped
    env_file:
      - .env
      - ./backend:/app
    ports:
      - "8000:8000"
